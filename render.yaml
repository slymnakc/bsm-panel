services:
  - type: web
    name: bsm-panel
    runtime: static
    plan: free
    branch: main
    autoDeploy: true
    buildCommand: echo "Static BSM panel ready"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
